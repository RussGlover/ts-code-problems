# Practice code problems with typescript

write solutions to code problems and test them with jest

## usage

A coding problem is in src/index.ts. Run the test command to run the tests with watch mode.

`npm test`
